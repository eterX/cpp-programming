#include <iostream>

int main(int argc, const char *argv[])
{
    /* this using directive tells the compiler 
	 * that we're using everything in the std namespace!
	 */
    using namespace std;

	cout << "Hello World!";
	return 0;
}
